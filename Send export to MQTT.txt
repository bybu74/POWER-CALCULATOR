alias: Send export to MQTT
description: ""
triggers:
  - entity_id: sensor.power_meter_active_power
    trigger: state
actions:
  - data:
      topic: panouri/export
      payload: "{{ states('sensor.power_meter_active_power') }}"
    action: mqtt.publish
mode: single